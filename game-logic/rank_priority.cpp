#include <iostream>
#include "longest_line.cpp"

class priority {

  public:
    /*
    int rank_count = 0;
    int longest_chain_value = longestChain();
    int crowning_length = crowning();
    int moves_from_center = safeMove();
    */
    //float longest_length = global;


};

int main(){

  determine_length find_longest_line = new determine_length;

  int row = 0;
  int column = 0;
  std::cin >> row;
  std::cin >> column;

  std::cout << determine_length.find_longest_line << std::endl; //input a place on the board to check for the longest line of the computer
  // or std::cout << determine_length << std::endl;
}
